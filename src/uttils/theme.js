import { colors } from "./colors";
import { fonts } from "./fonts";
import { variables } from "./variables";
import { handleProps } from "./handleStyled";
import { mediaWidths } from "./breakpoints";
import { mediaHeights } from "./breakpoints";

export const theme = {
  colors,
  fonts,
  variables,
  handleProps,
  mediaWidths,
  mediaHeights,
};
