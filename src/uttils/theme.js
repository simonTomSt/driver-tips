import { colors } from "./colors";
import { fonts } from "./fonts";
import { variables } from "./variables";
import { handleProps } from "./handleStyled";
export const theme = {
  colors,
  fonts,
  variables,
  handleProps,
};
